packages:
  github.com/authorhealth/events/v2:
    config:
      all: true
      dir: "{{ .InterfaceDir }}"
      filename: "mock_{{ .InterfaceNameSnake }}.go"
      inpackage: true
      recursive: true
      with-expecter: true
